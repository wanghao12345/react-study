/**
 * 笔记本
 * @param state 整个仓库存储的数据
 * @param action
 * @returns {*}
 */

const defaultState = {
  inputValue: '',
  list: [1, 2]
};

export default (state = defaultState, action) => {
  return state
}
